Retorna um fabricante específico confome o nome do fabricante passado.
---
tags:
  - Fabricantes

parameters:
  - in: path
    name: _id
    type: string
    required: true
    description: Id do documento.

responses:
  200:
    description:
    schema:
      $ref: '#/definitions/Fabricante'