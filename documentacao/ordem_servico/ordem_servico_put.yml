Atualiza a ordem de servico passando os dados de diagnostico clinico/tecnico.
---
tags:
  - Ordem Servicos
parameters:
  - in: body
    name: Equipamento
    schema:
        $ref: '#/definitions/Equipamento'

responses:
  200:
    description:
    schema:
      $ref: '#/definitions/Equipamento'