Register new service orders. 
---
tags:
  - Ordem Servicos

parameters: 
  - in: body
    name: Ordem de Servico
    description: You can send many objects with the fields you wish to create.
    schema:
      type: array
      items:
        $ref: '#/definitions/OrdemdeServicoPost'

responses:
  200:
    description: JSON
  