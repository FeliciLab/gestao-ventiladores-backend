Replaces the service order data according to the document id sent in the route. 
---
tags:
  - Ordem Servicos

parameters:
  - in: path
    name: _id
    type: string
    required: true
    description: Document Id.

  - in: body
    name: Ordem de Servico
    description: You can send the fields as you wish to replace.
    schema:
        $ref: '#/definitions/OrdemdeServico'

responses:
  200:
    description: Empty body.