Replaces the servie orders data according to the documents id sent in json body. 
---
tags:
  - Ordem Servicos

parameters:
  - in: body
    name: Ordem de Servico
    description: You can send many objects with the fields you wish to replace.
    schema:
      type: array
      items:
        $ref: '#/definitions/OrdemdeServico'

responses:
  200:
    description: Empty body.