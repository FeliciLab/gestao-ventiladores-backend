Register new item.
---
tags:
  - Items

parameters:
  - in: body
    name: Item
    description: You can send many objects with the fields you wish to create.
    schema:
      type: array
      items:
        $ref: '#/definitions/Item'

responses:
  200:
    description: JSON
  