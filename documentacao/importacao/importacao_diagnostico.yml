Import google form data for service order (diagnostic).
---
tags:
  - Imports

parameters:
  - in: body
    name: Import
    schema:
        $ref: '#/definitions/Import'

responses:
  200:
    description: Save in collection database service order the diagnostic data.