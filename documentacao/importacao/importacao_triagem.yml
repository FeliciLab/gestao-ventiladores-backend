Import google form data for service order (sort).
---
tags:
  - Imports

definitions:
  Import:
    type: object
    properties:
      url:
        type: string

parameters:
  - in: body
    name: Import
    schema:
        $ref: '#/definitions/Import'

responses:
  200:
    description: Save in collection database service order the sort data.