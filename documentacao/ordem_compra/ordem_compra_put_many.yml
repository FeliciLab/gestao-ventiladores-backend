Replaces the purchase orders data according to the documents id sent in json body. 
---
tags:
  - PurchaseOrders

parameters:
  - in: body
    name: PurchaseOrder
    description: You can send many objects with the fields you wish to replace.
    schema:
      type: array
      items:
        $ref: '#/definitions/PurchaseOrder'

responses:
  200:
    description: Empty body.