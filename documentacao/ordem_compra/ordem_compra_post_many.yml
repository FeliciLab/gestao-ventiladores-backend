Register new purchase orders. 
---
tags:
  - PurchaseOrders

parameters:
  - in: body
    name: PurchaseOrder
    description: You can send many objects with the fields you wish to create.
    schema:
      type: array
      items:
        $ref: '#/definitions/PurchaseOrder'

responses:
  200:
    description: JSON
  