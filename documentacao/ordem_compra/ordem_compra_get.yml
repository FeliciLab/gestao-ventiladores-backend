Retorna uma ordem de comra específica confome o id do documento passado.
---
tags:
  - Ordem Compras

parameters:
        - in: path
          name: _id
          type: string
          required: true
          description: Id do documento.

responses:
  200:
    description: Retorna os dados da ordem de servico registrada