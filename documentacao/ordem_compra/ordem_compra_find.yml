Returns all the purchase orders registered, according to the query sent. 
---
tags:
  - PurchaseOrders

parameters:
  - in: body
    description: You can send the fields you wish in "select" and "where".
    schema:
      properties:
        where:
          type: object
          properties:
            key:
              type: string
        select:
          type: array
          items:
            type: string

responses:
  200:
    description: JSON