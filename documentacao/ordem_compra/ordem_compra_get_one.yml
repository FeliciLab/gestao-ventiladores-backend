Returns a specific purchase order according to the document id sent in the route.
---
tags:
  - PurchaseOrders

parameters:
  - in: path
    name: _id
    type: string
    required: true
    description: Document Id.

responses:
  200:
    description: JSON
