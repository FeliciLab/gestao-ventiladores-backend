Retorna um equipamento específico confome o número de ordem de servico passado.
---
tags:
  - Equipamentos

parameters:
  - in: path
    name: _id
    type: string
    required: true
    description: Id do documento.

responses:
  200:
    description:
    schema:
      $ref: '#/definitions/Equipamento'