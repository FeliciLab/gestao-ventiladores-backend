Register new equipments. 
---
tags:
  - Equipamentos

parameters:
  - in: body
    name: Equipamento
    description: You can send many objects with the fields you wish to create.
    schema:
      type: array
      items:
        $ref: '#/definitions/EquipamentoPost'

responses:
  200:
    description: JSON
  