Updates only the fields sent in the json body, according to the documents id sent also in json body. 
---
tags:
  - Equipamentos

parameters:
  - in: body
    name: Equipamento
    description: You can send many objects with the fields you wish to update. The document id is required.
    schema:
      type: array
      items:
        $ref: '#/definitions/Equipamento'

responses:
  200:
    description: Empty body.