Replaces the movings data according to the documents id sent in json body. 
---
tags:
  - Movimentacoes

parameters:
  - in: body
    name: Movimentacao
    description: You can send many objects with the fields you wish to replace.
    schema:
      type: array
      items:
        $ref: '#/definitions/Movimentacao'

responses:
  200:
    description: Empty body.