Register new movings.
---
tags:
  - Movings

parameters: 
  - in: body
    name: Moving
    description: You can send many objects with the fields you wish to create.
    schema:
      type: array
      items:
        $ref: '#/definitions/Moving'

responses:
  200:
    description: JSON
  