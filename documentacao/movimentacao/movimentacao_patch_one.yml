Updates only the fields sent in the json body, according to the document id sent in the route. 
---
tags:
  - Movimentacoes
  
parameters:
  - in: path
    name: _id
    type: string
    required: true
    description: Document Id.

  - in: body
    name: Movimentacao
    description: You can send the fields as you wish to update.
    schema:
        $ref: '#/definitions/Movimentacao'

responses:
  200:
    description: Empty body.